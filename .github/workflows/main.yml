name: install.sh

on:
  push:
    branches: [ master ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - run: pwd
    - uses: actions/checkout@v2
    - name: install.sh
      run: ./install.sh
